// Datei: ThemeColors.kt
// Paket: com.example.terun
// Quelle: moco202613composablesmodifier.pdf — Verwendung konsistenter Farbwerte in Compose Color(0xFF...)

package com.example.terun

import androidx.compose.ui.graphics.Color

// Globale Farbkonstanten für ein einheitliches App-Design
val DarkBackground = Color(0xFF0D1B2A)  // Dunkles Blau-Schwarz für Screens
val TeRunBlue = Color(0xFF1A6FF5)       // Blau für Primär-Buttons und Markierungen
val MapDark = Color(0xFF1B2A3A)         // Dunkles Blau für die Karte
val TopBarDark = Color(0xFF101820)      // Sehr dunkles Grau für die TopBar
val BottomBarDark = Color(0xFF0B1118)   // Fast Schwarz für die Bottom Navigation

val SpotBlue = Color(0xFF4A8DFF)        // Blau für nicht erreichte Spots
val SpotOrange = Color(0xFFFFB547)      // Orange für eigene Spieler-Marker

val ActiveGreen = Color(0xFF56C596)     // Grün für erreichte Spots im aktiven Duell
val EnemyRed = Color(0xFFFF5A5A)        // Rot für gegnerische Spieler-Marker

val AufgebenRot = Color(0xFFD64545)     // Rotes Rot für den "Aufgeben"-Button
val BadgeGruen = Color(0xFF2E9E6B)      // Dunkleres Grün für den "2 / 3 Spots" Badge
